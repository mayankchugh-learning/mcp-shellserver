

# New history with no parents (orphan)
git switch --orphan project/langchain-mcp-adapters-clean
# Your working tree is kept; stage and commit as the first commit on this line
git add -A
git commit -m "Initial commit for langchain MCP adapters"
