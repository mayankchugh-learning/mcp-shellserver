[project]
name = "shellserver"
version = "0.1.0"
description = "MCP server that exposes a terminal tool for running shell commands"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.26.0",
]

[project.scripts]
shellserver = "server:main"
