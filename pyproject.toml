[project]
name = "shellserver"
version = "0.1.0"
description = "MCP server: shell tool, example.com and gist curl tools, Desktop mcpreadme resources"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.26.0",
]

[project.scripts]
shellserver = "server:main"
